# Hiemenz stagnation point

At $\beta = 1$ ($m = 1$), the Falkner-Skan equation describes stagnation-point flow
(Hiemenz flow). As $M_e \to 0$, the solver must recover the known wall shear
$f''(0)$ for this case.

*(coming soon)*

## Run

```bash
python vnv/verification/falkner_skan/hiemenz/verification_hiemenz.py
```
