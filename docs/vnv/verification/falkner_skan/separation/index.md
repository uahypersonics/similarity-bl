# Separation point

At $\beta \approx -0.1988$ ($m \approx -0.0904$), the Falkner-Skan solution
approaches separation where $f''(0) \to 0$.  Verifies the solver correctly
approaches zero wall shear at the Hartree separation parameter.

*(coming soon)*

## Run

```bash
python vnv/verification/falkner_skan/separation/verification_separation.py
```
